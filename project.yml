name: ${PRODUCT_NAME}
options:
  bundleIdPrefix: com.lastmile
  deploymentTarget:
    iOS: "16.0"
settings:
  base:
    SWIFT_VERSION: "5.9"
    PRODUCT_BUNDLE_IDENTIFIER: com.lastmile.app
targets:
  App:
    type: application
    platform: iOS
    sources: [Sources]
    settings:
      base:
        PRODUCT_NAME: App
        INFOPLIST_FILE: Sources/Info.plist
        DEVELOPMENT_TEAM: ""
